/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "export",
  basePath: "/weather-climate-data",
  images: { unoptimized: true },
};

export default nextConfig;
